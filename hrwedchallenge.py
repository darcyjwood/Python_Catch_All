if __name__ == '__main__':
    for _ in range(int(input())):
        name = input()
        score = float(input())
        
scores_list = [{'name' : name, 'score' :score}]       
scores = [{'name' : name, 'score' :score}]
score_list = sorted(scores)

for score in range(score_list)
        if score == score_list(list1)[-2]
        print(score)


if __name__ == '__main__':
    for _ in range(int(input())):
        name = input()
        score = float(input())
        
        
        scores_list = [{'name' : name, 'score' :score}]
        for score in 'scores_list':
            x = max('scores_list')



            i = x[-2]
            
                   

    for i in score:
        if i < lowest_score:
            next_lowest_score = lowest_score
            lowest_score = i
        elif i != lowest_score and i < next_lowest_score:
            next_lowest_score = i
        print(name, i)
        
            